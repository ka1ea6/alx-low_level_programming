#include "ops.h"

/**
 * mul - multiplies two numbers
 *
 * @a: first number
 * @b: second number
 * Return: the product of the first and second number.
 */

int mul(int a, int b)
{
	return (a * b);
}
