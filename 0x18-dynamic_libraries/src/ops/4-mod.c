#include "ops.h"

/**
 * mod - returns the modulus of the first number
 * by the second number
 *
 * @a: first number
 * @b: second number
 *
 * Return: result of modulo operator.
 */

int mod(int a, int b)
{
	return (a % b);
}
